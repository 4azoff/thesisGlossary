version: "3.9"
networks:
  network:
    driver: bridge

services:
  terms:
    build: .

    container_name: terms
    ports:
      - "8080:8080"
    networks:
      - network
    user: root
    restart: on-failure